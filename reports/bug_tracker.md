# Bug Tracker

| ID | Date | Type | Symptom | Seed/Case | Root Cause | Fix/Status |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | 2026-07-09 | Environment | WSL2 system Python had no pytest | `python3 -m pytest tests` | Dependencies were installed in project `.venv`, not system Python | Use `.venv/bin/python -m pytest tests`; CLOSED |
| BUG-002 | 2026-07-09 | Verification gap | Scoreboard did not compare dirty writeback side effect | Dirty eviction directed case | `CacheResponse` lacked writeback address/data fields | Added writeback fields and mismatch checks; CLOSED |
| BUG-003 | 2026-07-09 | Invocation | `python -m cache_vip.regression` failed before install | Core regression CLI | Source layout requires `src` on `PYTHONPATH` unless package is installed | Documented `PYTHONPATH=src` source-tree command and editable install option; CLOSED |
| BUG-004 | 2026-07-10 | DUT adapter bug | CPU response ready handshake missing in `ToffeeCacheAdapter` | DUT smoke tests | `sample_cpu_response` acquired `ready_sig` but never drove it; docstring said "Pull ready high" but code omitted | Added `ready_sig.write(1) → wait_cycles(1) → ready_sig.write(0)`; CLOSED |
| BUG-005 | 2026-07-10 | DUT adapter bug | Reset signal never asserted in `RealDUTWrapper` | Real DUT smoke tests | `_reset` called `reset_wrapper.write()` without `await`, coroutine never executed | Added `await` on both `write(1)` and `write(0)` calls; CLOSED |
| BUG-006 | 2026-07-10 | Regression bug | Enhanced regression report generation throws `KeyError` | `run_enhanced_regression` with `report_dir` | `_format_markdown` accessed `summary["coverage"]`/`summary["crv"]` but enhanced summary has `coverage_summary`/`core_regression` keys | Split formatter into `_format_core_markdown` and `_format_enhanced_markdown`, dispatch by key presence; CLOSED |
| BUG-007 | 2026-07-10 | Memory agent bug | ToffeeMemoryAgent hardcodes line size to 64 bytes | Non-64B line configurations | `monitor_memory_request` and `handle_fill` both used literal `64` instead of parameter | Added `line_bytes` constructor parameter (default 64), both methods use `self.line_bytes`; CLOSED |
| BUG-008 | 2026-07-10 | Regression gap | Enhanced regression DUT mode declared but not implemented | `run_enhanced_regression(dut_seeds=...)` | `dut_seeds`/`dut_count` parameters accepted but never used; `dut_regression` list always empty | Added DUT seed loop (reuses enhanced core seed runner with dut name prefix); CLOSED |
| BUG-009 | 2026-07-10 | Verification logic | CRV seed 5 false positive at txn 877 | `_run_named_stream` CRV streams | READ data validation compared against `written` dict that does not track eviction; after eviction+refill the READ returns memory data, not the tracked write, producing a false mismatch | Added `verify_read_data` parameter; CRV streams use `verify_read_data=False` (determinism + coverage only), directed/smoke streams retain value-level verification; CLOSED |
| BUG-010 | 2026-07-10 | RTL design bug | LRU replacement policy incorrectly implemented in `NutShellCache.v` | Directed LRU eviction sequence (set_idx=3) | On cache hit, `lru_way[index]` is set to `hit_way + 1` instead of tracking recency; fill also does not advance a victim pointer | OPEN (source-level finding). The current directed test proves the required behavior only on the reference model. Closure requires a failing RealNutShellCache transaction trace that records the actual victim tag. |
| BUG-011 | 2026-07-10 | RTL design bug | Dirty eviction has no writeback-to-fill phase | Dirty victim followed by a miss to a new line | The same memory-response handler treats a writeback acknowledgement as fill data; there is no explicit writeback → fill state transition | OPEN (source-level finding). Closure requires a RealNutShellCache bus trace showing writeback, the missing/incorrect fill, and timeout or wrong CPU response. |
| BUG-012 | 2026-07-13 | RTL design bug | Write-miss CPU data can be lost after fill | Reset; write miss A; memory fill response; read A | The simplified RTL fill handler installs memory data but does not merge the pending CPU `req_wdata/req_wmask` for a write miss | OPEN (source-level finding). Add a real DUT failing test and waveform before claiming dynamic confirmation. |
